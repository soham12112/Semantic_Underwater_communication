# Core dependencies
# Use opencv-contrib-python for tracker support (CSRT, KCF, etc.)
# If trackers aren't needed, opencv-python works too
opencv-contrib-python>=4.8.0
numpy>=1.24.0

# Image captioning
transformers>=4.35.0
torch>=2.0.0
Pillow>=10.0.0

# LLM integration
requests>=2.28.0  # For MiniMax API
openai>=1.0.0     # Optional: OpenAI provider
anthropic>=0.18.0 # Optional: Anthropic provider

# Utilities
tqdm>=4.65.0
pydantic>=2.0.0

# Optional: GPU acceleration for captioning
# torch-cuda>=2.0.0  # Uncomment if CUDA available
